# 更新日志

本项目遵循语义化版本。

## [0.2.0] — 2026-09-15

面向商用（SaaS 自运营）的安全与可靠性加固，围绕一次系统性审计的发现逐项修复。

### 新增
- **找回密码**：忘了密码可以凭注册手机号 + 短信验证码直接重置，不用再靠管理员手动改密。
- **备份脚本**：`scripts/backup.py`（`npm run backup`）一键导出 MySQL + 打包应用数据目录，
  可以直接进 cron / 计划任务；之前只有文档里的手动命令。

### 安全加固
- 生产配置校验现在会精确识别并拒绝 `docker-compose.yml` 里为了让 demo 零配置跑通而写死
  的那两个公开密钥（`JWT_SECRET_KEY` / `LLM_ENCRYPTION_KEY`），即使格式合法也不放行——
  之前只挡占位符样式的值，挡不住"看起来是真密钥、其实全网都知道"这种。
- 登录、注册接口新增按 IP / 按用户名的限流，防撞库；此前只有聊天、上传等接口有限流。
- `scripts/seed_demo.py` 增加生产环境防呆：`APP_ENV=production` 时直接拒绝执行，不会在真实
  数据库里建出一个密码是 `demo12345` 的账号。

### 可靠性
- 向量库支持 Server 模式（`CHROMA_SERVER_HOST`）：API 和 Worker 是两个独立进程，内嵌
  `PersistentClient` 不保证多进程并发读写安全；docker-compose 已内置一个 `chroma` 服务，
  非 Docker 部署可用 `chroma run` 自己起一个。未配置时仍回退到原来的内嵌模式，本地开发不受影响。

## [0.1.0] — 2026-09-10

首个可部署版本。核心链路（知识库对话）可用，工程基建齐备。

### 功能
- **用户与权限**：注册 / 登录 / JWT，管理员后台（用户 · 任务 · 使用统计 · 操作日志 · 系统诊断）。
- **Agent 对话**：自建智能体（模型 / 提示词 / RAG / 记忆开关），ReAct 引擎 + 工具调用，SSE 流式回答。
- **知识库 RAG**：上传 PDF/Word/TXT/MD → 可调切块 → 向量化（远程 API 或本地 BGE）→ ChromaDB → rerank
  → `【来源N】` 引用，回答正文和来源芯片都能点开看原文片段；上下文压缩 / Token 节省统计。
- **知识库空间与治理**：多文档空间、跨空间联合检索；成员角色（owner/admin/editor/viewer）、操作审计、
  管理员企业视角；健康分、调试台、评估集 + RAG 自动评分。
- **长期记忆**：按 Agent 维度自动总结沉淀。
- **可视化工作台**：选模板填字段建小窗口（知识库健康 / 检索抽查 / 用量 / 概览 / 内部接口 /
  阈值告警 / 网页监控 / 网页快照 / 每日 AI 简报 / 联网查指标），自然语言创建作为兜底；后台定时跑、
  攒历史、阈值告警。
- **网页监控**：抓取公开网页正文入库或盯更新，带 SSRF 防护。
- **模型连接**：智谱 / DeepSeek / OpenAI / Kimi / 通义 / Perplexity；「一次连接」自动配好聊天 + 向量两条能力。

### 工程
- 聊天与 RAG 检索链路全量 `AsyncSession`（LangGraph 同步引擎经 `to_thread` + `asyncio.Queue` 桥接）。
- 领域异常层 + 统一处理器；外部调用统一超时 / 重试 / 指数退避 / 进程内熔断；生产启动配置强校验。
- 334 个测试（含 TestClient + 真 JWT + 真 DB 的路由级隔离测试）；CI 跑全测 + 前端构建。
- Prometheus `/metrics`；`bootstrap_database` 幂等建表。
- `docker compose up` 一键起（MySQL + Redis + API + Worker + 前端）；`scripts/seed_demo.py` 离线演示数据。

### 已知限制
- 定位偏「什么都做」，部分功能仍在够用状态（联网检索为弱兜底，工作台自然语言创建准确率有限）。
- 缺企业管道：SSO / 组织级多租户 / 消息推送集成。
- 未经真实场景长期打磨。
